import SwiftUI

/// Minimal shared theme. Kept intentionally small — see docs/roadmap.md,
/// "Non-goals for this phase"; this app is built for EVE, not as a themeable
/// general-purpose client.
enum EVETheme {
    static let accent = Color.accentColor
}
